{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}

 

<div class="container mt-5">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4>Modifier un utilisateur</h4>
                    <a href="{{ path('inscription.read') }}" class="btn btn-success">Liste des Utilisateurs</a>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}

                    <div class="class mb-3">
                        {{ form_row(form.prenom, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="class mb-3">
                        {{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    
                    <div class="class mb-3">
                        {{ form_row(form.profile, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="class mb-3">
                        {{ form_row(form.email, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    
                    <div class="class mb-3">
                        {{ form_row(form.password, {'attr': {'class': 'form-control'}}) }}
                    </div>

                   

                   <div class="class mb-3">
                      {{ form_row(form.inscrire, {'label': 'Modifier', 'attr': {'class': 'btn btn-primary'}}) }}
                   </div>
                  

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
